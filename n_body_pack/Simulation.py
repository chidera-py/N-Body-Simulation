class Simulation:
	def _init__(self, num_particles, gride_size, dt):
		self.N = num_particles
		self.grid_size = grid_size
		self.dt = dt
		self.particles = self.initialize_particles()

	def initialize_particles(self):
		particles = []
		pass
